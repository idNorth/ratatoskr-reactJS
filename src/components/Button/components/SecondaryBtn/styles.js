import styled from "styled-components";

// styles
import { TagA, TagBtn, TagLink } from "../../styles/common";

export const StyledTagBtn = styled(TagBtn)``;

export const StyledTagA = styled(TagA)``;

export const StyledTagLink = styled(TagLink)``;
